# Design draft — The Family House

Visual direction only. Not wired to PINs, bookings, or family save.

Open `draft/index.html` (or visit `/draft/` on a local server).

## Direction
- Full-bleed Provençal coastal hero instead of a centred login card
- Brand name as the main signal on first view
- Sea / pine / limestone palette (moves away from cream + terracotta card UI)
- Fraunces + Outfit type
- Same jobs: enter PIN, create PIN, then calendar / house inside

The working sync fix remains on branch `cursor/fix-phone-sync-globals-8b93`.
